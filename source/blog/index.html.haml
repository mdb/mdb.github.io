%h1 Blog
%ul
- blog('blog').articles.each do |article|
  %li
    %h2
      %a{:href => article.url}=article.title
    %a{:href => article.url}
      %img{:src => image_path("blog/thumbnails/#{article.data.thumbnail}")}
