<h1>Posts</h1>
<ul>
  <% for post in @posts %>
  <li>
    <h2><%= link_to post.title, post.permalink %></h2>
    <p><%= post.content %></p>
    <% if post.tag_list.length >= 1 %>
      <p>Tags: <%= raw post.tag_list.map { |t| link_to t, tag_path(t) }.join(', ') %></p>
    <% end %>
  </li>
  <% end %>
</ul>

<%= link_to 'New Post', new_post_path %>
