name: Hello

on:
  repository_dispatch:
    types: ["hello"]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say-Hello
        run: echo "Hello, ${{ github.event.client_payload.name }}!"
