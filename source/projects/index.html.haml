%h1 Projects
%ul
  - blog('projects').articles.each do |article|
    %li
      %h2
        %a{:href => article.url}=article.title
      %a{:href => article.url}
        %img{:src => project_thumb_path(article.data.thumbnail)}
